export const createAnimePaths = (id: string | number) => {
 return {
    fullAnime: `/anime/${id}`,
  };
}
export const createMangaPaths = (id: string | number) => {
 return {
    fullManga: `/manga/${id}`,
  };
}